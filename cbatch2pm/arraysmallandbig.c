#include<stdio.h>
main()
{
	int i,j,k,small,big,n,a[20];
	printf("Enter the size of that array");
	scanf("%d",&n);
	printf("enter the elements in that array");
	
	for(i=0;i<n;i++)
		if(a[i]>big)
		{
			big=a[i];
		}
		else if(a[i]<small)
		{
			small=a[i];
		}
		
	}
	printf("%d is big and %d is small",big,small)
	scanf("%d",&a[i]);
	small=a[0];
	big=a[0];
	for(i=0;i<n;i++)
	{;
		
}
